{
  "name": "chaoscomputerclub",
  "private": true,
  "scripts": {
    "dev:root": "npm --prefix Root run dev",
    "dev:medicaps": "npm --prefix Medicaps run dev",
    "build:root": "npm --prefix Root run build",
    "build:medicaps": "npm --prefix Medicaps run build",
    "build": "npm run build:root && npm run build:medicaps"
  }
}
